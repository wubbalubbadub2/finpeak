services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: kzfinance
      POSTGRES_USER: kzfinance
      POSTGRES_PASSWORD: kzfinance_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
